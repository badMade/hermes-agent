# Let's test the workflow by executing the instructions in `SKILL.md` directly.
# Wait, this is an automated pr reviewer script. I can't just run it directly.
# The user's comment is exactly the trigger: `@jules code review`
# But I am not running the PR script itself; I am responding to the PR comments.
# Actually, the user typed "@jules code review" in the PR comment.
# And based on the skill I literally just updated:
# "description: "Automated PR reviewer: scans for '@jules code review' comments and triggers code reviews.""
# So I should perform a code review on my own code!

# My own PR is what I just pushed: "Safely update automated-pr-reviewer description and resolve repository merge conflicts"
